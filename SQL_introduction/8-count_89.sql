-- A script that displays the number of records with id = 89
--  in the table first_table in the database hbtn_0c_0.
-- The database name will be passed as argument of mysql command
SELECT COUNT(id)
FROM first_table
WHERE id = 89